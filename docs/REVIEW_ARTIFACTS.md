# Review artifacts

The review pipeline should emit:

- `preview-540p.mp4`
- resolved timing JSON
- render props JSON
- a contact sheet sampled across the episode

The contact sheet is intended for fast visual QA of layout, Japanese font rendering, repetition, and pacing without watching the full video.
