import React,{useMemo} from "react";
import {Player} from "@remotion/player";
import fixture from "../../fixtures/v2.1-demo.json";
import type {EpisodeManifest} from "../contracts/types";
import {EpisodeVideo} from "../composition/EpisodeVideo";
import {buildDemoResolved} from "../fixtures/buildDemoResolved";
const manifest=fixture as EpisodeManifest;
export const App:React.FC=()=>{const resolved=useMemo(()=>buildDemoResolved(manifest),[]);return <main style={{minHeight:"100vh",background:"#E8ECEC",color:"#152B3C",fontFamily:"Inter,Arial,sans-serif",padding:24,boxSizing:"border-box"}}><header style={{maxWidth:1440,margin:"0 auto 20px"}}><h1 style={{fontSize:28,margin:0}}>World in Clear English — Preview</h1><p style={{margin:"8px 0 0",color:"#425569"}}>v2.1 fixture · silent timing fixture only · not production audio</p></header><section style={{maxWidth:1440,margin:"0 auto",display:"grid",gridTemplateColumns:"minmax(220px,320px) minmax(0,1fr)",gap:20}}><aside style={{background:"#fff",borderRadius:12,padding:16,alignSelf:"start"}}><strong>{manifest.episodeId}</strong><div style={{fontSize:13,color:"#425569",marginTop:8}}>revision {manifest.revision}<br/>{manifest.scenes.length} scenes<br/>{resolved.durationFrames} frames</div><ol style={{paddingLeft:22,marginBottom:0}}>{manifest.scenes.map(scene=><li key={scene.id} style={{margin:"8px 0"}}>{scene.id}</li>)}</ol></aside><div style={{background:"#102A43",padding:10,borderRadius:12}}><Player component={EpisodeVideo} inputProps={{manifest,resolved}} durationInFrames={resolved.durationFrames} compositionWidth={resolved.width} compositionHeight={resolved.height} fps={resolved.fps} controls initiallyShowControls style={{width:"100%",aspectRatio:"16 / 9",background:"#F7F5EF"}}/></div></section></main>;};
